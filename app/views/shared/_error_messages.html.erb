<!-- καλύτερα ν αλλάξει ονομασία να ξεχωρίζει ότι μιλά για χρήστες -->
<% if @user.errors.any? %>
<div id="error_explanation">
  <h2>Ουπς. Κάτι πήγε λάθος.</h2>
  
  <ul>
    <% @user.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

